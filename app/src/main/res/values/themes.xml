<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HomZen" parent="Theme.Material3.DayNight.NoActionBar">
        // primary colors
        <item name="colorPrimary">@color/lavender</item>
        <item name="colorPrimaryVariant">@color/lavender</item>
        <item name="colorOnPrimary">@color/white</item>

        //secondary colors
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        //status bar colors
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="TransparentDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>

    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/divider</item>
        <item name="android:layout_marginStart">56dp</item>
    </style>

    <style name="ThemeOverlay.CardView" parent="">
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
    </style>

    <style name="FilterChipStyle" parent="Widget.MaterialComponents.Chip.Filter">
        <item name="chipStrokeColor">#3F51B5</item>
        <item name="chipBackgroundColor">@android:color/white</item>
        <item name="chipStrokeWidth">2dp</item>
    </style>

    <style name="Theme.HomZen" parent="Base.Theme.HomZen" />
</resources>